(function(){function boot(){if(document.getElementById('radioWorkflowPanel'))return;const grid=document.querySelector('#adminApp .grid');if(!grid)return;const panel=document.createElement('section');panel.id='radioWorkflowPanel';panel.className='panel';panel.style.gridColumn='1 / -1';panel.innerHTML='<h2>Radio Workflow / Station Assets</h2><p class="small">Podcast, voiceover, station drop, and track order panel.</p><div class="empty">Uploader and ordering controls will connect here.</div>';grid.appendChild(panel);}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();window.addEventListener('ub-firebase-ready',boot);})();